@model AP5PW_Helpdesk.ViewModels.Auth.RegisterVM
@{
    ViewData["Title"] = "Register";
    Layout = null;
}

<!doctype html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - AP5PW_Helpdesk</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- JetBrains Mono -->
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&display=swap" rel="stylesheet">

    <!-- Material Icons -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,600,1,0&icon_names=bug_report" />

    <style>
        :root {
            --bg: #0b0f14;
            --card: rgba(15,20,30,.35);
            --card-border: rgba(255,255,255,.14);
            --card-glow: rgba(102,16,242,.35);
            --text: #e2e8f0;
            --muted: #9aa6b2;
            --brand1: #0d6efd;
            --brand2: #6610f2;
        }

        html, body {
            height: 100%;
        }

        /* ===== BACKGROUND + NOISE ===== */
        body {
            margin: 0;
            font-family: "JetBrains Mono",monospace;
            color: var(--text);
            display: flex;
            align-items: center;
            justify-content: center;
            background: radial-gradient(900px 600px at 20% 10%, rgba(13,110,253,.22), transparent 60%), radial-gradient(1000px 700px at 80% 95%, rgba(102,16,242,.20), transparent 60%), var(--bg);
            position: relative;
            overflow: hidden;
        }

            body::before {
                content: "";
                position: fixed;
                inset: 0;
                pointer-events: none;
                opacity: .08;
                mix-blend-mode: overlay;
                background-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='140' height='140' viewBox='0 0 140 140'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='2' stitchTiles='stitch'/></filter><rect width='100%' height='100%' filter='url(%23n)' opacity='0.5'/></svg>");
                background-size: 220px 220px;
                animation: grain 8s steps(10) infinite;
            }
        @@keyframes grain {
            0%

        {
            transform: translate(0,0)
        }

        10% {
            transform: translate(-3%,2%)
        }

        20% {
            transform: translate(2%,-1%)
        }

        30% {
            transform: translate(1%,3%)
        }

        40% {
            transform: translate(-2%,-2%)
        }

        50% {
            transform: translate(3%,1%)
        }

        60% {
            transform: translate(-1%,0)
        }

        70% {
            transform: translate(2%,-3%)
        }

        80% {
            transform: translate(-3%,2%)
        }

        90% {
            transform: translate(1%,-2%)
        }

        100% {
            transform: translate(0,0)
        }

        }

        .bottom-glow {
            position: fixed;
            left: 50%;
            bottom: -140px;
            width: 1200px;
            height: 500px;
            transform: translateX(-50%);
            background: radial-gradient(closest-side, rgba(102,16,242,.28), transparent 70%);
            filter: blur(12px);
            pointer-events: none;
        }

        /* ===== CARD ===== */
        .register-card {
            width: min(92vw,520px);
            background: var(--card);
            border: 1px solid var(--card-border);
            border-radius: 1rem;
            padding: 2.6rem 2rem 2.3rem;
            text-align: center;
            backdrop-filter: blur(20px) saturate(160%);
            -webkit-backdrop-filter: blur(20px) saturate(160%);
            box-shadow: 0 20px 60px rgba(0,0,0,.55), inset 0 0 22px rgba(255,255,255,.05);
            animation: fadeUp .7s ease both;
        }
        @@keyframes fadeUp {
            from

        {
            opacity: 0;
            transform: translateY(16px)
        }

        to {
            opacity: 1;
            transform: translateY(0)
        }

        }

        .logo {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 80px;
            height: 80px;
            margin: -8px auto 10px;
            border-radius: 20px;
            background: linear-gradient(135deg, rgba(13,110,253,.25), rgba(102,16,242,.25));
            box-shadow: 0 0 40px rgba(102,16,242,.4);
        }

            .logo .material-symbols-outlined {
                font-size: 68px;
                color: #bca8ff;
                filter: drop-shadow(0 0 12px rgba(102,16,242,.7));
            }

        h2 {
            font-weight: 800;
            color: #fff;
            letter-spacing: .02em;
            margin: .4rem 0 1.6rem;
        }

        label {
            font-weight: 600;
            font-size: .86rem;
            color: #cbd5e1;
        }

        .form-control,
        .form-select {
            background: rgba(255, 255, 255, 0.06);
            border: 1px solid rgba(255, 255, 255, 0.16);
            color: #fff;
            border-radius: 0.55rem;
            padding: 0.7rem 0.9rem;
            outline: none;
            transition: 0.2s border-color, 0.2s box-shadow, 0.2s background;
            appearance: none;
            -webkit-appearance: none;
            -moz-appearance: none;
            position: relative;
            cursor: pointer;
        }

            /* Add custom arrow */
            .form-select::after {
                content: "▾";
                position: absolute;
                right: 12px;
                top: 50%;
                transform: translateY(-50%);
                color: rgba(255,255,255,0.6);
                pointer-events: none;
            }

        /* Dropdown fix for dark mode */
        select option {
            background-color: #0f141b;
            color: #e2e8f0;
            padding: 0.5rem;
            border: none;
        }

            /* Highlight selected item */
            select option:checked,
            select option:hover {
                background-color: rgba(102,16,242,0.3);
                color: #fff;
            }


            .form-control:focus, .form-select:focus {
                border-color: #7a35ff;
                box-shadow: 0 0 0 .2rem rgba(122,53,255,.18);
                background: rgba(255,255,255,.08);
            }

        .btn-register {
            background: linear-gradient(135deg, var(--brand1), var(--brand2));
            color: #fff;
            font-weight: 800;
            border: none;
            border-radius: .55rem;
            padding: .7rem 1rem;
            width: 100%;
            margin-top: .8rem;
            letter-spacing: .02em;
            transition: .2s transform,.25s box-shadow;
        }

            .btn-register:hover {
                transform: translateY(-1px);
                box-shadow: 0 0 22px rgba(102,16,242,.45);
            }

        .btn-back {
            margin-top: .8rem;
            background: none;
            border: 1px solid rgba(255,255,255,.25);
            color: #fff;
            font-weight: 600;
            border-radius: .55rem;
            padding: .6rem 1rem;
            width: 100%;
            transition: .2s;
        }

            .btn-back:hover {
                background: rgba(255,255,255,.08);
                border-color: rgba(255,255,255,.4);
            }

        .validation-summary-errors {
            background: rgba(220,53,69,.10);
            border: 1px solid rgba(220,53,69,.25);
            border-radius: .5rem;
            padding: .75rem;
            margin-bottom: 1rem;
            color: #ff7a8a;
            font-size: .9rem;
            text-align: left;
        }
    </style>
</head>
<body>

    <div class="bottom-glow"></div>

    <div class="register-card">
        <div class="logo">
            <span class="material-symbols-outlined">bug_report</span>
        </div>
        <h2>Register</h2>

        <form asp-action="Register" method="post" novalidate>
            @Html.AntiForgeryToken()

            <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>

            <div class="mb-3 text-start">
                <label asp-for="UserName" class="form-label"></label>
                <input asp-for="UserName" class="form-control" autocomplete="username" />
                <span asp-validation-for="UserName" class="text-danger small"></span>
            </div>

            <div class="mb-3 text-start">
                <label asp-for="LastName" class="form-label"></label>
                <input asp-for="LastName" class="form-control" />
                <span asp-validation-for="LastName" class="text-danger small"></span>
            </div>

            <div class="mb-3 text-start">
                <label asp-for="Password" class="form-label"></label>
                <input asp-for="Password" class="form-control" type="password" autocomplete="new-password" />
                <span asp-validation-for="Password" class="text-danger small"></span>
            </div>

            <div class="mb-3 text-start">
                <label asp-for="ConfirmPassword" class="form-label"></label>
                <input asp-for="ConfirmPassword" class="form-control" type="password" autocomplete="new-password" />
                <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
            </div>

            <div class="mb-3 text-start">
                <label asp-for="CompanyId" class="form-label"></label>
                <select asp-for="CompanyId" class="form-select" asp-items="Model.CompanyList">
                    <option value="">-- select company (optional) --</option>
                </select>
                <span asp-validation-for="CompanyId" class="text-danger small"></span>
            </div>

            <button type="submit" class="btn-register">Register</button>
            <a asp-action="Login" class="btn-back d-block text-center mt-2">Back to login</a>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <partial name="_ValidationScriptsPartial" />
</body>
</html>
